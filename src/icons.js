/**
 * WordPress dependencies
 */
import { listItem, postList } from '@wordpress/icons';

// A list of sites, and one item of it. Not the link icon, the toolbar
// has that one for the link overlay.
export const blogroll = postList;
export const link = listItem;
